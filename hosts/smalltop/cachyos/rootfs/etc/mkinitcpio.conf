# Busybox init on purpose (runtime hooks need it); nixguard sits after encrypt, before resume.
MODULES=(btrfs)
BINARIES=()
FILES=()
HOOKS=(base udev autodetect microcode modconf kms keyboard keymap consolefont block encrypt lvm2 nixguard resume filesystems)
COMPRESSION="zstd"
