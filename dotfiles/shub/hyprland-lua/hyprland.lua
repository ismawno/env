-- Entry point. host.lua next to this file names the profile (laptop or desktop) and carries the machine's facts.
require("shub.settings")
require("shub.startup")
require("shub.rules")
require("shub.keybinds")
