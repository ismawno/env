# hyprpaper 0.8 dropped preload= and the one-line wallpaper=; * matches all monitors, and "current" is whatever wallpaper-pick pinned.

wallpaper {
    monitor = *
    path = ~/.config/backgrounds/current
}

splash = false

ipc = on
