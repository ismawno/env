# Replace with location of your wallpaper
# hyprpaper 0.8 dropped preload= and the one-line wallpaper=; * matches all monitors.

wallpaper {
    monitor = *
    path = ~/.config/backgrounds/1.png
}

splash = false

ipc = on
